@model u22560646_HW3.Models.HomeViewModel
@{
    ViewBag.Title = "Customers";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container mt-4">
    <div class="card shadow-sm h-100">
        <div class="card-header bg-primary text-white d-flex justify-content-between align-items-center">
            <strong>Customers</strong>
            <button class="btn btn-light btn-sm" data-bs-toggle="modal" data-bs-target="#createCustomerModal">
                Create
            </button>
        </div>

        <div class="card-body p-0">
            @if (Model.PagedCustomers != null && Model.PagedCustomers.Any())
            {
                <table class="table table-sm table-striped mb-0">
                    <thead>
                        <tr>
                            <th>Name</th>
                            <th>Email</th>
                            <th>Phone</th>
                            <th>City</th>
                            <th>Zip Code</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var c in Model.PagedCustomers)
                        {
                            <tr>
                                <td>@c.FirstName @c.LastName</td>
                                <td>@c.Email</td>
                                <td>@c.Phone</td>
                                <td>@c.City</td>
                                <td>@c.ZipCode</td>
                            </tr>
                        }
                    </tbody>
                </table>
            }
            else
            {
                <p class="p-3 text-muted">No customers found.</p>
            }
        </div>

        
        <nav aria-label="Customer pagination" class="mt-2">
            <ul class="pagination pagination-sm justify-content-center mb-0">
                @{
                    int current = ViewBag.CustPage;
                    int total = Model.PagedCustomers.PageCount;
                    int start = Math.Max(1, current - 2);
                    int end = Math.Min(total, current + 2);
                }

                @if (current > 1)
                {
                    <li class="page-item">
                        <a class="page-link" href="@Url.Action("Customer", new { custPage = 1 })">&laquo;</a>
                    </li>
                    <li class="page-item">
                        <a class="page-link" href="@Url.Action("Customer", new { custPage = current - 1 })">&lt;</a>
                    </li>
                }

                @if (start > 1)
                {
                    <li class="page-item disabled"><span class="page-link">...</span></li>
                }

                @for (int i = start; i <= end; i++)
                {
                    <li class="page-item @(i == current ? "active" : "")">
                        <a class="page-link" href="@Url.Action("Customer", new { custPage = i })">@i</a>
                    </li>
                }

                @if (end < total)
                {
                    <li class="page-item disabled"><span class="page-link">...</span></li>
                }

                @if (current < total)
                {
                    <li class="page-item">
                        <a class="page-link" href="@Url.Action("Customer", new { custPage = current + 1 })">&gt;</a>
                    </li>
                    <li class="page-item">
                        <a class="page-link" href="@Url.Action("Customer", new { custPage = total })">&raquo;</a>
                    </li>
                }
            </ul>
        </nav>
    </div>
</div>


<div class="modal fade" id="createCustomerModal" tabindex="-1">
    <div class="modal-dialog">
        <div class="modal-content">
            @using (Html.BeginForm("CreateCustomer", "Home", FormMethod.Post))
            {
                @Html.AntiForgeryToken()
                <div class="modal-header">
                    <h5 class="modal-title">Create Customer</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                </div>
                <div class="modal-body">
                    @Html.TextBox("FirstName", null, new { @class = "form-control mb-2", placeholder = "First Name" })
                    @Html.TextBox("LastName", null, new { @class = "form-control mb-2", placeholder = "Last Name" })
                    @Html.TextBox("Email", null, new { @class = "form-control mb-2", placeholder = "Email" })
                    @Html.TextBox("Phone", null, new { @class = "form-control mb-2", placeholder = "Phone" })
                    @Html.TextBox("City", null, new { @class = "form-control mb-2", placeholder = "City" })
                    @Html.TextBox("ZipCode", null, new { @class = "form-control", placeholder = "Zip Code" })
                </div>
                <div class="modal-footer">
                    <button type="submit" class="btn btn-success">Save</button>
                </div>
            }
        </div>
    </div>
</div>

